# C6 Maven remediation candidate — 2026-09-08

Local ordinary builds and the full five-project Trivy inventory pass the HIGH/CRITICAL gate. The authenticated hosted Sonatype audit for this candidate is **pending**. Historical C2–C5 hosted reports remain unchanged; this document neither replaces their outcomes nor grants risk acceptance.

The [machine-readable evidence](../infrastructure/runtime-security/C6_MAVEN_REMEDIATION_2026-09-08.json) records exact source, artifact, raw-report and SBOM hashes.

| Component | Concrete change | Verification |
| --- | --- | --- |
| Site derivative | Replace Apache parent release/report/check automation with explicit project dependencies and required build goals. Keep all Site goal implementations, help/descriptor generation, manifest and Apache LICENSE/NOTICE. | All 128 project dependency paths retain the same versions/scopes. Ordinary 4 tests pass. All 60 packaged entries except the embedded POM remain byte-identical. Source archive provenance remains 60 files: 55 unchanged, 5 reviewed patches. |
| Necessary plugin tools | Help uses Xstream 1.4.21/Plexus 4.0.3; Shade uses Plexus 3.6.2; PluginPlugin and RemoteResources use Aircompressor 2.0.3/Plexus 4.0.3. | Real clean install succeeds; bridge 7 tests and Site 4 tests pass. All executable classes/resources/descriptors of bridge, Site and shaded transport are unchanged. |
| Standalone Keycloak provider | Pin the effective build tools and provided Netty BOM 4.1.137.Final/protobuf-java 3.25.8; keep Keycloak 26.7.3 and Java 21. | Actual Temurin 21 build and 4 tests pass. All 12 classes match the original C5 Java 21 build; only three Maven/manifest metadata entries change. Provided libraries are not bundled into the provider JAR. |
| Complete audit runner | Add only the fixed `--standalone-project keycloak` repository seam, with GAV and release-21 checks; preserve all analyzer flags/cache/fail-closed behavior. Validate active reporting and extension pins too. | 24 causal tests pass, including wrong project/release, missing/stale reports and complete-scope flag propagation. Existing four-project audits continue after individual failures without repeating completed projects. |
| Checkout provenance | Normalize the five origin-checked CRLF files to the configured LF checkout and regenerate their downstream hashes and archive patch. | The original clean checkout fails provenance; corrected clean checkouts with both Git newline policies pass 60/55/5. Reapplying the patch reproduces all 60 files. An offline Site rebuild passes four tests and preserves all runtime and test class bytes. |

The final local inventory contains **484 unique JARs** across all project scopes and actual active build/report/extension plugin closures for the aggregate, audit bridge, Site, transport and standalone issuer. The raw scan reports **0 HIGH, 0 CRITICAL and 5 MEDIUM**. All findings and the full SBOM are retained. The earlier four-bootstrap baseline had 9 HIGH/3 CRITICAL and the issuer baseline had 22 HIGH/2 CRITICAL; these overlapping graph counts must not be added as unique findings.

Aircompressor 2.0.3 retains the upstream Java-8 baseline in its [official POM](https://repo.maven.apache.org/maven2/io/airlift/aircompressor/2.0.3/aircompressor-2.0.3.pom). Netty 4.1.137 is an [official bug-fix and security release](https://netty.io/news/2026/08/06/4-1-137-Final.html). These versions were verified in the actual build and graph inventory, not accepted solely from version metadata.

No advisory suppression, analyzer disabling, severity threshold reduction or private repository was introduced. Removing Apache-specific publication and source-formatting automation from this locally installed derivative does not remove a Site product goal. The hosted required audit must still cover backend plus four bootstrap projects plus the standalone issuer with its real authenticated analyzer result. Local Trivy success is not a claim of hosted completion or a blanket security guarantee.

The checkout correction follows the recorded scans and full backend validation. Those historical JAR and report digests remain unchanged in the evidence. The later Site-only rebuild has a separate digest: its sole packaged delta is CRLF-to-LF normalization of the embedded POM; the other 59 entries, including all 26 runtime classes, are identical. No source semantics, dependency versions, vulnerability policy or analyzer coverage changed.
