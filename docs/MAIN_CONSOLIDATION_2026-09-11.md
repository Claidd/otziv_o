# Сведение рабочего релиза в main — 11 сентября 2026

Основой служит `0ce9a34aab2d4e60d0d82872fc7ce1dba2aff1b7`: проверенный стек
архитектурных исправлений, очередей доставки, общей модели клиентов и согласованных
образов инфраструктуры. Код приложения, установленный на VPS перед объединением,
соответствует `7b5d653f14ac01a6e3392fce73cc437ca5774647`. Само объединение не
выполняет новый деплой.

До изменения основной папки сохранены её HEAD, индекс, refs, двоичные патчи,
изменённые и новые файлы. Копия проверена чтением архива и сверкой SHA-256 файлов.
Приватные диагностические данные и архивы образов остаются вне публичного Git.

## Результат сравнения

| Источник | Решение |
| --- | --- |
| `architecture-correction-20260908`, `whatsapp-delivery-20260909`, `manager-daily-summary`, `manager-gamification`, `hardening-r0-r10`, `analytics-aggregates` | Уже входят в историю выбранного релиза; повторное применение не требуется. |
| `architecture-remediation-20260907` / PR #2 | Альтернативный ранний вариант. 1318 файлов совпадают с релизом, 123 соответствуют его прежним версиям, ещё 10 изменений поглощены. Остальные варианты заменены типизированными результатами доставки, очередями, публичными API модулей, актуальными образами и их тестами. Старые прямые отправки и старые образы не возвращаются. |
| `risk179-telegram-commit` | Настройки ограничения действий сотрудников и обработка ошибок сохранены в новых API. Захваченный субъект команды заменяет чтение thread-local аутентификации; Telegram использует очередь. Старые реализации не восстанавливаются. |
| `origin/detached3` | Ранние изменения уже включены; репозиторий отложенных сообщений дополнен захватами, бюджетом отправок и безопасным восстановлением. |
| Локальные загрузчик приложения и открытие оплаты | Перенесены с сохранением актуальных HTTP-interceptors. Открытие публичной оплаты не ждёт silent SSO; при ошибке загрузки видна кнопка обновления. |
| Локальные MinIO server experiments и отчёт второй волны | Сохранены в приватной копии. В рабочем релизе используется другая проверенная реализация S3; экспериментальные файлы не являются входами её сборки. |
| `performance-slo` / `release-safety` (`569231f7`) | Сохранены отдельными ветками. 156 файлов — самостоятельная ещё не установленная переработка: кэши, read models, асинхронное обновление, метрики браузера. Миграции 215–217 конфликтуют с уже занятой нумерацией. Для переноса нужны новые миграции, согласование API и отдельные проверки на копии БД. Эти функции не объявляются включёнными в текущий релиз. |

Ветки сохраняются как источники истории. Для основной разработки и обычного
деплоя используется `main`; изменения проходят PR и обязательные проверки.
Команда и её ограничения описаны в [DEPLOYMENT.md](DEPLOYMENT.md).
