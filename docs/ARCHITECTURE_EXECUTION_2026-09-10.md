# Исполнение архитектурного плана — 11 сентября 2026

База: `ffa33d85ed0414b3796a0d730853ad4755d16905`, ветка `codex/architecture-completion-20260910`, [PR #5](https://github.com/Claidd/otziv_o/pull/5) поверх PR #4. Это текущий статус новой волны. Предыдущие отчёты и доказательства публикаций сохраняют свой исторический смысл.

**Новые изменения ещё не установлены на VPS.** Штатный локальный Docker smoke на свежей копии production БД прошёл, включая повтор после обновления Maven-инструментов. На `26c22b8f` все семь запусков CI завершились успешно, включая полный backend и мониторинг. Последующие изменения браузерных образов и уменьшение deploy bundle требуют собственного прогона. Подготовка выпуска прервана эксплуатационным инцидентом, описанным ниже.

| Этап | Выполнено в коде и проверено | Остаток |
|---|---|---|
| 1. Актуальное состояние | Сверены прежние 26 подтверждений, включая исходные 13; неоднозначные операции отделены от разработки | Сверка после установки, окончательная привязка исходников/образов/БД |
| 2. Доставка | Постоянные зашифрованные очереди общих счетов, manager send/reply и legacy WhatsApp; ограниченные попытки, квитанции для UNKNOWN, статусы web/mobile | Установка миграций 311–313 и приёмка очередей |
| 3. Выпуск | Все семь C15 образов опубликованы и включены в Compose; восстановлена воспроизводимость Maven-кэша; добавлены фиксация digest, локальная сборка WhatsApp и проверка места | CI, фактический комплект приложения, расчёт места для всего выпуска, backup/rollback и установка |
| 4. A02 | CompanyOrderInputs query API; решение о конкурирующем банковском платеже принадлежит payments API; удалены соответствующие исключения ArchUnit | Приёмка выбранных сценариев на установленном выпуске; другие исторические границы не объявляются исправленными |
| 5. A09 | Выделены ScheduledDeliveryRecovery и подготовка текста публикации; модели и наблюдение за доставкой в клиентах | Сравнение горячих сценариев и эксплуатационная приёмка |
| 6. A12 | Общий DeliveryOperation, 188 операций / 205 схем; feature API использует собственные модели; защита от старых ответов | Прежний Android signing key, подписанное обновление и явно определённая поддерживаемая версия |
| 7. Эксплуатация | Восемь семейств очередей в защищённом сборщике, четыре правила Prometheus; точный PostgreSQL разрешён инструментами восстановления | Независимый хост/получатель тревог, полный комплект восстановления и согласованные RPO/RTO |

## Доставка и совместимость

Операция хранит неизменяемые адресат, текст и ID. Бизнес-транзакция заканчивается до обращения к провайдеру. UNKNOWN и истёкшая аренда разрешают только чтение существующей квитанции; отсутствие записи не разрешает повторную отправку.

Повтор известного отказа до отправки ограничен и требует актуальных полномочий автора и исходного состояния. Изменённый заказ, сумма или входящее сообщение останавливают устаревшую команду. Поздняя положительная квитанция сохраняется без перезаписи нового бизнес-состояния. Успех без ID сообщения остаётся неопределённым.

Новые клиенты передают `X-Otziv-Delivery-Protocol: queued-v1` на четырёх командах. Старый клиент, считающий любой 2xx подтверждением отправки, получает 409 с объяснением уже сохранённой операции; эта проверка не откатывает очередь. Формат и поведение опубликованной Android-73 проверены, но это не заменяет проверку подписанного обновления. Новый ключ взамен потерянного не создавался.

## Проверки

Числа пересекающихся наборов не суммируются.

- Общий backend CI на `76c03119`: 4 904 теста, одна ошибка сравнения сохранённого отпечатка `backend/pom.xml`, 15 пропущено. Контракт затем заново экспортирован через настоящий Java exporter, его MVC/Jackson проверки прошли; единственное изменение экспорта — хеш POM, wire-форма прежняя. Полный повтор CI обязателен.
- Доставка/карточки: 121 PASS; общие счета: 278 PASS; очереди/метрики с MySQL: 24 PASS. A02: 285 PASS, A09: 67 PASS, включая транзакции и конкурентные случаи.
- Web: 753 теста и production build PASS. Mobile: 200 runtime-тестов, 231 Node-тест и production build PASS. Дополнительно проверены настоящие экраны: ошибка GET не вызывает повторный POST. Полные npm audit после Hono 4.13.5 и Vitest 4.1.11: 0 замечаний.
- Штатное восстановление production-дампа и `prod-like-smoke.ps1`: PASS; повтор после последнего изменения Maven также PASS. SHA-256 дампа: `70e7b300b6854f474ff93ae880b6da94e9935a452aee883d54c047fb920855ac`. Внешние отправки отключены. Вход, роли, readiness, критические API и финансовые ограничения проверены.
- Zstd JNI обновлён до 1.5.7-14 в затронутых Maven plugin realms и Site. Производный Site: `3.22.0-otziv-jetty12.0.39-2`. Bootstrap, effective policy, 30 тестов аудитора, происхождение 60 файлов и настоящее выполнение Site прошли. Удалённый Dependency audit `34522505676`: PASS.
- Secret scanning `34522505815`: PASS для истории PR. Исключения публичных метаданных ограничены точными путями/строками; семь контрольных случаев прошли. Не отключены ни детекторы, ни обязательность проверки.
- Архив чистого `76c03119`: все 32 ссылки образов, полное доказательство Alloy и инфраструктурный контракт PASS. Для доказательств больше не требуется локальный игнорируемый YAML.
- Runtime regression после дополнительного разбора PostgreSQL: 617 PASS. Keycloak C15: 52 проверки исходного перехода + 20 issuer-проверок PASS. MC: 22 PASS при ограничении 512 MiB; исходная неудача при 192 MiB сохранена.
- Recovery: 25 PASS. Полная изолированная репетиция с текущими MySQL, PostgreSQL, Keycloak и точным app-образом прошла: четыре восстановленных компонента, вход/refresh, отозванные старые сессии, расшифровка и версия файла; восстановление заняло 84 секунды на синтетическом наборе, что не является production RTO. Защита БД и расчёт места: 61 PASS; контракт штатного деплоя PASS. Реальный запуск расчёта от пользователя VPS подтвердил чтение диска/размера БД, распознавание установленного config digest и отказ при заведомой нехватке места, без изменения работающих контейнеров. Это проверка расчётчика, не допуск всего выпуска.

## Новое замечание PostgreSQL

В свежем сканировании появился CVE-2026-74860 для Python SAX-привязок libxml2. [Debian](https://security-tracker.debian.org/tracker/CVE-2026-74860) указывает исправление в 2.15.3. Проверенный upstream-архив 2.15.4 содержит исправленный Python-код, а наш рецепт использует `--without-python`.

Дополнительная проверка повторно экспортировала остановленный точный образ и сверила все 2 136 путей с первоначальным доказательством. Интерпретатор, Python runtime и libxml2mod отсутствуют. Решение применяется только к этой CVE, пакету/версии и проверенному составу; подмена библиотеки, источника, PURL, образа, доказательства или срока отклоняется. Исходный отчёт с 28 HIGH/CRITICAL сохранён; эффективных неподтверждённых замечаний после проверки нет. Старый C14 adjudicator и свидетельства публикации не переписаны; дополнительная проверка живёт отдельно.

## Штатный деплой и место

Поддерживаемая точка входа остаётся `infrastructure/scripts/prod/deploy-prod.ps1`. Изменённый WhatsApp и Docker observer собираются и публикуются локально; на VPS выполняется загрузка готовых образов. Перед упаковкой ссылки приложения/web/WhatsApp фиксируются по digest. Активируется проверенный Keycloak default, чтобы старое значение env не оставило прежний образ незаметно.

Расчёт места измеряет распакованные tar-слои, сверяет их SHA-256 с diff ID опубликованной OCI-конфигурации и отдельно учитывает размер скачивания. Docker inspect Size не используется как распакованный размер. Повторно используются только полные совпадающие цепочки слоёв из реально установленных образов; изменённый родитель не даёт ложного совпадения. Для новых слоёв заложены скачивание, две распаковки и файловые накладные расходы, для выпуска — одновременные файлы нового/старого комплекта и резервная копия БД. Резерв 1 GiB сохраняется на каждом используемом файловом разделе. Для одного раздела потребности суммируются; существующие образы не удаляются. Проверка выполняется до паузы автозапуска и снова перед распаковкой выпуска. Модель рассчитана на проверенный overlay2; другой storage driver требует отдельной проверки. Размер SQL-backup оценочный и не гарантирует будущий рост БД/логов.

Полный расчёт слоёв выпуска `26c22b8f` выполнен: одновременное размещение комплекта не проходит проверку места. Этот прогноз консервативно включал неактивный worker, phpMyAdmin/certbot и весь Git-архив вместо deploy bundle. Новый bundle сокращён до 209 файлов / примерно 7 MB; в него входит только используемый chromium-seccomp.json, а не сотни мегабайт исходников и свидетельств аудита. Окончательный допуск требует пересчёта фактического комплекта; снижать резерв ради PASS нельзя. Старые образы не удалены.

## VPS и оставшиеся внешние данные

На проверке 11 сентября в 03:34 по Иркутску: 14 основных сервисов healthy, phpMyAdmin выключен, таймер активен; оба WhatsApp подключены, входящие pending/dead=0, ожиданий блокировок MySQL=0. В 04:03 свободно около 3,13 GiB. Миграции 311–313 ещё не установлены.

Исходные 13 отправок уже входят в ранее восстановленные 26. Закрытая плановая задача с положительной квитанцией ожидает нового правила сохранения позднего подтверждения; неоднозначные записи без достаточного доказательства остаются на сверке. Реальные тестовые сообщения и слепые повторы не выполнялись.

Расположение прежнего Android-ключа и независимый получатель мониторинга запрошены у владельца и пока не получены. Эти пункты остаются открытыми. Основной checkout с существующим index.lock сохранён. Частные дампы, реквизиты и журналы находятся вне Git; точка продолжения — `ARCHITECTURE_COMPLETION_STATUS.json` в служебном каталоге основного checkout.


## Инцидент подготовки 11 сентября, 05:18–05:30 по Иркутску

Попытка сохранить пять неиспользуемых образов напрямую вне VPS была ошибочной: Docker save сначала создаёт временные tar-слои на сервере. Экспорт временно заполнил диск. MySQL ожидал освобождения места для binlog, файловый журнал приложения получил ошибку записи, оба входящих WhatsApp inbox перешли в защитное состояние. Ошибка вызвана выбранной операцией подготовки, а не новым релизом: релиз не установлен.

Остановлен только принадлежавший операции клиент экспорта; Docker daemon не перезапускался. Временное место освободилось, более 3 GiB снова доступны. Старые образы, тома и записи БД не удалялись. Для снятия ошибки хранения перезапущены существующие два шлюза с теми же образами и /auth. Оба аккаунта требуют QR-входа с телефонов; это запрошено у владельца. Число исходящих записей осталось 98/153, новых UNKNOWN не появилось. После входа требуется дождаться восстановления доступной входящей истории и повторно проверить очереди.

Все 326 таблиц MySQL прочитаны в согласованном read-only snapshot: 1 072 357 строк, ожидающих транзакций и data lock waits нет. Это проверка доступности данных, а не доказательство отсутствия любой потери за интервал сбоя. Свежая копия БД перенесена потоком без временного файла на VPS, проверена по SHA-256/CRC и успешно восстановлена штатным локальным скриптом. На этой копии прошёл полный prod-like smoke, включая миграции 311–313, вход, frontend/API и локальные защитные настройки. Инструмент экспорта дополнен проверкой запаса для временных слоёв и watchdog; повторный экспорт на VPS не запускался.

## Текущий браузерный кандидат

Прототипы worker/WhatsApp используют общий слой Node 22 / Debian Trixie и Google Chrome 153.0.8010.36-1 из официального репозитория, закреплённый SHA-256. Проверены реальный sandbox, Puppeteer local/remote lifecycle и worker OCR/readiness/drain без входа в провайдера и без реальной рассылки. Версия запущенного браузера сверяется с закреплённой; vendor metadata сохранены в runtime-security/proofs/chrome153.

В прототипах остаются 80 HIGH и 5 CRITICAL системных package rows без доступного исправления по Debian feed; окончательный WhatsApp image даёт те же числа. Это не нулевой результат и не принятие риска. Trivy Debian scan не доказывает полноту покрытия Google Chrome: vendor browser advisories проверяются отдельно. Новый observer на Node 24 Alpine прошёл реальных Dozzle/Alloy consumers и имеет 0 HIGH/CRITICAL в записанном сканировании прототипа. Финальные три образа собраны, оба browser runtime smoke и реальные observer consumers прошли; 17 Node contracts и 61 deployment guard test прошли. CI новой версии и окончательные результаты worker/observer scan остаются отдельными проверками; deployment approval не заявлено.
