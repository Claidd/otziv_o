*** Begin Patch
*** Update File: backend/src/main/resources/db/migration/V1_10_252__manual_payment_task_actual_destination.sql
@@
-SET invoice.payment_route_manual_task_source_generation = CONCAT('LEGACY-', invoice.id),
+SET invoice.payment_route_manual_task_source_generation = CONCAT('LEGACY-', invoice.invoice_id),
*** Update File: backend/src/main/java/com/hunt/otziv/payments/service/PaymentLinkService.java
@@
     private void retireStaleReusableLink(PaymentLink link) {
         taskReceiptIntegrationService.release(link,
                 "Платежная ссылка пересоздана из-за изменения суммы или маршрута оплаты");
-        taskReceiptIntegrationService.release(link,
-                "Платежная ссылка пересоздана из-за изменения суммы или маршрута оплаты");
*** Update File: backend/src/main/java/com/hunt/otziv/payments/service/ManualPaymentTaskLedgerService.java
@@
         return entry.getSourceKind() == source.sourceKind()
-                && entry.getSourceId() == source.sourceId()
+                && java.util.Objects.equals(entry.getSourceId(), source.sourceId())
                 && entry.getSourceGeneration().equals(source.sourceGeneration());
*** End Patch
