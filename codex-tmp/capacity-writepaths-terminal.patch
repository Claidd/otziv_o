*** Begin Patch
*** Update File: backend/src/main/java/com/hunt/otziv/common_billing/service/CommonManualPaymentAttributionCoordinator.java
@@
-import com.hunt.otziv.contractor_payments.service.ContractorPaymentAccountingService;
-import com.hunt.otziv.contractor_payments.service.ContractorRewardLedgerService;
 import com.hunt.otziv.contractor_payments.service.ContractorPaymentTargetAccessPolicy;
+import com.hunt.otziv.payments.service.ManualPaymentTaskContractorCapacityService;
@@
-    private final ContractorPaymentAccountingService accountingService;
-    private final ContractorRewardLedgerService ledgerService;
+    private final ManualPaymentTaskContractorCapacityService taskCapacityService;
@@
-        long reserved = allocationRepository.sumOutstandingExposure(
-                profile.getId(), mode, OUTSTANDING
-        );
-        long netReceived = Math.max(
-                0L,
-                accountingService.confirmedGross(profile, mode) - accountingService.returned(profile, mode)
-        );
-        long base = Math.max(0L, ledgerService.totalAccrued(profile) - netReceived - reserved);
+        long base = taskCapacityService.ordinaryAvailable(profile, mode);
*** Update File: backend/src/main/java/com/hunt/otziv/contractor_payments/service/ContractorDirectSettlementService.java
@@
 import com.hunt.otziv.contractor_payments.repository.ContractorPaymentProfileRepository;
+import com.hunt.otziv.payments.service.ManualPaymentTaskContractorCapacityService;
@@
-    private final ContractorPaymentProfileService profileService;
+    private final ManualPaymentTaskContractorCapacityService taskCapacityService;
@@
-        long available = profileService.available(profile, mode);
+        long available = taskCapacityService.ordinaryAvailable(profile, mode);
*** Update File: backend/src/main/java/com/hunt/otziv/payments/service/ManualPaymentTaskContractorCapacityService.java
@@
-        if (!counts(task)) {
+        if (!isProfileTarget(task)) {
             return task == null || task.getId() == null
                     ? TaskCommitmentSnapshot.NONE
                     : new TaskCommitmentSnapshot(task.getId(), null, 0L, 0L);
@@
-        long commitment = addSaturated(
-                commitment(task.getTargetAmountKopecks(), confirmed, pending),
-                unbackedConfirmed);
-        long acknowledged = hasAuditableAcknowledgement(task)
+        boolean terminal = task.getStatus() == ManualPaymentTaskStatus.COMPLETED
+                || task.getStatus() == ManualPaymentTaskStatus.CANCELED;
+        long commitment = terminal
+                ? unbackedConfirmed
+                : addSaturated(
+                        commitment(task.getTargetAmountKopecks(), confirmed, pending),
+                        unbackedConfirmed);
+        long acknowledged = !terminal && hasAuditableAcknowledgement(task)
                 ? nonNegative(task.getTargetOverrunAcknowledgedKopecks()) : 0L;
@@
     private boolean counts(ManualPaymentTask task) {
-        if (task == null || task.getAccountingTargetProfile() == null
-                || task.getAccountingTargetProfile().getId() == null) {
+        if (!isProfileTarget(task)) {
             return false;
         }
-        ManualPaymentTaskAccountingTargetKind kind = task.getAccountingTargetKind();
         ManualPaymentTaskStatus status = task.getStatus();
-        return (kind == ManualPaymentTaskAccountingTargetKind.SPECIALIST
-                || kind == ManualPaymentTaskAccountingTargetKind.MANAGER)
-                && status != ManualPaymentTaskStatus.COMPLETED
+        return status != ManualPaymentTaskStatus.COMPLETED
                 && status != ManualPaymentTaskStatus.CANCELED;
     }
+
+    private boolean isProfileTarget(ManualPaymentTask task) {
+        if (task == null || task.getAccountingTargetProfile() == null
+                || task.getAccountingTargetProfile().getId() == null) {
+            return false;
+        }
+        ManualPaymentTaskAccountingTargetKind kind = task.getAccountingTargetKind();
+        return kind == ManualPaymentTaskAccountingTargetKind.SPECIALIST
+                || kind == ManualPaymentTaskAccountingTargetKind.MANAGER;
+    }
*** End Patch
