*** Begin Patch
*** Update File: backend/src/main/java/com/hunt/otziv/payments/service/ManualPaymentTaskAccountingTargetPolicy.java
@@
 import com.hunt.otziv.contractor_payments.service.ContractorPaymentTargetAccessPolicy;
 import com.hunt.otziv.payments.dto.ManualPaymentTaskAccountingTargetOption;
+import com.hunt.otziv.payments.dto.ManualPaymentTaskBalance;
 import com.hunt.otziv.payments.model.ManualPaymentTask;
@@
-        if (!profile.isEnabled() || profile.getUser() == null || profile.getUser().getId() == null) {
+        if (!eligible(profile, accountingMode)
+                || profile.getUser() == null || profile.getUser().getId() == null) {
             throw conflict("Профиль получателя отключён");
         }
@@
-        long available = Math.max(0, profileService.available(profile, accountingMode));
-        available = addOwnPendingExposure(available, profile, existingTaskId);
-        long overrun = Math.max(0, subtractSaturated(targetAmountKopecks, available));
+        ExistingTaskExposure exposure = existingTaskExposure(existingTaskId);
+        long available = Math.max(0, profileService.available(profile, accountingMode));
+        available = addOwnPendingExposure(available, profile, exposure);
+        long remainingToFund = remainingToFund(targetAmountKopecks, exposure);
+        long overrun = Math.max(0, subtractSaturated(remainingToFund, available));
         if (overrun > 0 && !overrunAcknowledged) {
@@
         List<ManualPaymentTaskAccountingTargetOption> result = new ArrayList<>();
-        result.add(option(ManualPaymentTaskAccountingTargetKind.EXTERNAL_TASK, null, target, 0));
-        result.add(option(ManualPaymentTaskAccountingTargetKind.OWNER, null, target, 0));
+        result.add(option(ManualPaymentTaskAccountingTargetKind.EXTERNAL_TASK, null,
+                target, target, 0, true));
+        result.add(option(ManualPaymentTaskAccountingTargetKind.OWNER, null,
+                target, target, 0, true));
         ContractorAllocationMode accountingMode = accountingPhaseService.current();
+        ExistingTaskExposure exposure = existingTaskExposure(taskId);
+        long remainingToFund = remainingToFund(target, exposure);
         for (ContractorPaymentProfile profile : profiles) {
             long available = Math.max(0, profileService.available(profile, accountingMode));
-            available = addOwnPendingExposure(available, profile, taskId);
+            available = addOwnPendingExposure(available, profile, exposure);
             ManualPaymentTaskAccountingTargetKind kind = profile.getRole() == ContractorRole.SPECIALIST
                     ? ManualPaymentTaskAccountingTargetKind.SPECIALIST
                     : ManualPaymentTaskAccountingTargetKind.MANAGER;
-            result.add(option(kind, profile, target, available));
+            result.add(option(kind, profile, target, remainingToFund, available,
+                    eligible(profile, accountingMode)));
         }
         return List.copyOf(result);
     }
 
-    private long addOwnPendingExposure(long available, ContractorPaymentProfile profile, Long taskId) {
-        if (taskId == null || profile == null || profile.getId() == null) {
+    private long addOwnPendingExposure(
+            long available,
+            ContractorPaymentProfile profile,
+            ExistingTaskExposure exposure
+    ) {
+        if (profile == null || profile.getId() == null
+                || exposure == null
+                || !profile.getId().equals(exposure.profileId())) {
             return available;
         }
-        ManualPaymentTask task = taskRepository.findByIdWithDetails(taskId).orElse(null);
-        if (task == null || task.getAccountingTargetProfile() == null
-                || !profile.getId().equals(task.getAccountingTargetProfile().getId())) {
-            return available;
-        }
         try {
-            return Math.addExact(available, ledgerService.balance(taskId).pendingAmountKopecks());
+            return Math.addExact(available, exposure.pendingAmountKopecks());
         } catch (ArithmeticException exception) {
             return Long.MAX_VALUE;
         }
     }
+
+    private ExistingTaskExposure existingTaskExposure(Long taskId) {
+        if (taskId == null || taskId <= 0) {
+            return ExistingTaskExposure.NONE;
+        }
+        ManualPaymentTask task = taskRepository.findByIdWithDetails(taskId).orElse(null);
+        if (task == null) {
+            return ExistingTaskExposure.NONE;
+        }
+        ManualPaymentTaskBalance balance = ledgerService.balance(taskId);
+        Long profileId = task.getAccountingTargetProfile() == null
+                ? null : task.getAccountingTargetProfile().getId();
+        return new ExistingTaskExposure(
+                profileId,
+                Math.max(0, balance.pendingAmountKopecks()),
+                Math.max(0, balance.netConfirmedAmountKopecks())
+        );
+    }
+
+    private long remainingToFund(long target, ExistingTaskExposure exposure) {
+        long confirmed = exposure == null ? 0 : exposure.netConfirmedAmountKopecks();
+        return Math.max(0, subtractSaturated(target, confirmed));
+    }
+
+    private boolean eligible(ContractorPaymentProfile profile, ContractorAllocationMode mode) {
+        return profile != null
+                && profile.isEnabled()
+                && (mode != ContractorAllocationMode.LIVE || profile.isLiveEnabled());
+    }
 
     private ManualPaymentTaskAccountingTargetOption option(
             ManualPaymentTaskAccountingTargetKind kind,
             ContractorPaymentProfile profile,
             long target,
-            long available
+            long remainingToFund,
+            long available,
+            boolean enabled
     ) {
-        long overrun = profile == null ? 0 : Math.max(0, subtractSaturated(target, available));
+        long overrun = profile == null
+                ? 0 : Math.max(0, subtractSaturated(remainingToFund, available));
         Long profileId = profile == null ? null : profile.getId();
@@
-                profile == null || profile.isEnabled(),
+                enabled,
                 available,
@@
     public record TargetResolution(
             ManualPaymentTaskAccountingTargetKind kind,
             ContractorPaymentProfile profile,
             String label,
             long currentAvailableKopecks,
             long projectedOverrunKopecks,
             boolean acknowledgementUsed
     ) {
     }
+
+    private record ExistingTaskExposure(
+            Long profileId,
+            long pendingAmountKopecks,
+            long netConfirmedAmountKopecks
+    ) {
+        private static final ExistingTaskExposure NONE = new ExistingTaskExposure(null, 0, 0);
+    }
 }
*** Update File: backend/src/main/java/com/hunt/otziv/payments/service/ManualPaymentTaskService.java
@@
     public ManualPaymentTaskResponse updateManagerTaskStatus(
             Long userId,
             Long taskId,
             String status,
             String actor
     ) {
+        ContractorAllocationMode accountingMode = contractorPaymentAccountingPhaseService.lockCurrent();
         ManualPaymentTask task = taskByIdForUpdate(taskId);
         assertTaskOwner(userId, task);
-        return updateStatus(task, status, actor);
+        return updateStatus(task, status, actor, accountingMode);
     }
@@
     public ManualPaymentTaskResponse updateManagementTaskStatus(
             Long taskId,
             String status,
             String actor
     ) {
-        return updateStatus(taskByIdForUpdate(taskId), status, actor);
+        ContractorAllocationMode accountingMode = contractorPaymentAccountingPhaseService.lockCurrent();
+        return updateStatus(taskByIdForUpdate(taskId), status, actor, accountingMode);
     }
@@
         if (manager == null
                 || manager.getId() == null
@@
             return Optional.empty();
         }
+        ContractorAllocationMode accountingMode = contractorPaymentAccountingPhaseService.lockCurrent();
 
         return manualPaymentTaskRepository
                 .findActiveForRouting(manager.getId(), profile.getId(), ManualPaymentTaskStatus.ACTIVE)
                 .stream()
-                .filter(task -> isRoutable(task, amountKopecks, excludedLinkId))
+                .filter(task -> isRoutable(task, amountKopecks, excludedLinkId, accountingMode))
                 .findFirst();
@@
-    private ManualPaymentTaskResponse updateStatus(ManualPaymentTask task, String status, String actor) {
+    private ManualPaymentTaskResponse updateStatus(
+            ManualPaymentTask task,
+            String status,
+            String actor,
+            ContractorAllocationMode accountingMode
+    ) {
         ManualPaymentTaskStatus newStatus = parseStatus(status);
         if (newStatus == ManualPaymentTaskStatus.ACTIVE) {
             validatePaymentTarget(task);
-            validateAccountingTargetForActivation(task);
+            validateAccountingTargetForActivation(task, accountingMode);
         }
@@
-    private boolean isRoutable(ManualPaymentTask task, long amountKopecks, Long excludedLinkId) {
+    private boolean isRoutable(
+            ManualPaymentTask task,
+            long amountKopecks,
+            Long excludedLinkId,
+            ContractorAllocationMode accountingMode
+    ) {
         if (task.getStatus() != ManualPaymentTaskStatus.ACTIVE
                 || !hasPaymentTarget(task)
-                || !hasResolvedAccountingTarget(task)
+                || !hasResolvedAccountingTarget(task, accountingMode)
                 || task.getTargetAmountKopecks() <= 0) {
@@
         ManualPaymentTaskAccountingTargetKind accountingKind = targetKind(task);
         ContractorPaymentProfile targetProfile = task.getAccountingTargetProfile();
-        long targetAvailable = targetAvailable(targetProfile, balance.pendingAmountKopecks());
+        ContractorAllocationMode accountingMode = contractorPaymentAccountingPhaseService.current();
+        long targetAvailable = targetAvailable(
+                targetProfile, balance.pendingAmountKopecks(), accountingMode);
+        long remainingToFund = Math.max(0,
+                saturatedSubtract(task.getTargetAmountKopecks(), confirmed));
         long targetOverrun = targetProfile == null ? 0
-                : Math.max(0, saturatedSubtract(task.getTargetAmountKopecks(), targetAvailable));
+                : Math.max(0, saturatedSubtract(remainingToFund, targetAvailable));
@@
-                isRoutableForAnyAmount(task, remaining),
+                isRoutableForAnyAmount(task, remaining, accountingMode),
@@
-    private boolean isRoutableForAnyAmount(ManualPaymentTask task, long remaining) {
+    private boolean isRoutableForAnyAmount(
+            ManualPaymentTask task,
+            long remaining,
+            ContractorAllocationMode accountingMode
+    ) {
         return task.getStatus() == ManualPaymentTaskStatus.ACTIVE
                 && remaining > 0
                 && hasPaymentTarget(task)
-                && hasResolvedAccountingTarget(task);
+                && hasResolvedAccountingTarget(task, accountingMode);
     }
@@
-    private void validateAccountingTargetForActivation(ManualPaymentTask task) {
-        if (!hasResolvedAccountingTarget(task)) {
+    private void validateAccountingTargetForActivation(
+            ManualPaymentTask task,
+            ContractorAllocationMode accountingMode
+    ) {
+        if (!hasResolvedAccountingTarget(task, accountingMode)) {
             throw new ResponseStatusException(
@@
-    private boolean hasResolvedAccountingTarget(ManualPaymentTask task) {
+    private boolean hasResolvedAccountingTarget(
+            ManualPaymentTask task,
+            ContractorAllocationMode accountingMode
+    ) {
         ManualPaymentTaskAccountingTargetKind kind = targetKind(task);
@@
-            return profile != null && profile.isEnabled() && profile.getRole() == expected;
+            return profile != null
+                    && profile.isEnabled()
+                    && (accountingMode != ContractorAllocationMode.LIVE || profile.isLiveEnabled())
+                    && profile.getUser() != null
+                    && profile.getUser().getId() != null
+                    && profile.getRole() == expected;
         }
@@
-    private long targetAvailable(ContractorPaymentProfile profile, long ownPending) {
+    private long targetAvailable(
+            ContractorPaymentProfile profile,
+            long ownPending,
+            ContractorAllocationMode accountingMode
+    ) {
         if (profile == null) {
             return 0;
         }
         long available = Math.max(0,
                 contractorPaymentProfileService.available(
-                        profile, contractorPaymentAccountingPhaseService.current()));
+                        profile, accountingMode));
*** End Patch
