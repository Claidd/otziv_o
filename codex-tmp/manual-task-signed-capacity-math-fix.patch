*** Begin Patch
*** Update File: backend/src/main/java/com/hunt/otziv/payments/service/ManualPaymentTaskContractorCapacityService.java
@@
-        long unacknowledgedOther = Math.max(0L, subtractSaturated(
-                otherCommitment, otherAcknowledged));
-        long capacityForTask = subtractSaturated(position, unacknowledgedOther);
+        // Acknowledgement is signed deficit coverage, not merely a discount
+        // bounded by the remaining commitment. It must therefore also cover a
+        // negative raw position created by the task's already persisted
+        // allocations/confirmations.
+        long capacityForTask = addSaturated(
+                subtractSaturated(position, otherCommitment),
+                otherAcknowledged
+        );
*** Update File: backend/src/main/java/com/hunt/otziv/contractor_payments/service/ContractorActualPaymentAttributionService.java
@@
-                    : capacity(actualProfile, mode, reallocate ? null : original);
+                    : capacity(actualProfile, mode, reallocate ? null : original, false);
*** End Patch
