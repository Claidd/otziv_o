*** Begin Patch
*** Update File: frontend/src/app/features/admin/tbank-payments/tbank-payments.component.html
@@
-  <section class="analytics-section recipient-summary-section" aria-label="Фактические ручные оплаты по получателям">
+  <section class="analytics-section recipient-summary-section" aria-label="Подтверждённые ручные оплаты по получателям до возвратов">
@@
-        <h2>Фактические получатели ручных оплат</h2>
+        <h2>Подтверждённые ручные оплаты · до возвратов</h2>
@@
-          <p>Кому фактически зачтены подтверждённые оплаты за выбранный месяц.</p>
+          <p>Кому первоначально зачтены подтверждённые оплаты; последующие возвраты здесь не вычитаются.</p>
@@
-            <th>Сумма</th>
+            <th>Подтверждено до возвратов</th>
*** Update File: mobile/src/app/features/tbank.page.ts
@@
-            <section class="recipient-list" aria-label="Факт оплат по получателям">
+            <section class="recipient-list" aria-label="Подтверждённые оплаты по получателям до возвратов">
@@
-                    {{ recipientMonthlySummary()?.totalPayments || 0 }} платежей
+                    {{ recipientMonthlySummary()?.totalPayments || 0 }} платежей · до возвратов
@@
-                        <small>Сумма</small>
+                        <small>Подтверждено до возвратов</small>
*** Update File: mobile/test/tbank-payment-task-safety.test.mjs
@@
 test('monthly recipient summary is fail-soft and outside the main bootstrap Promise.all', () => {
@@
   assert.match(webTbankSource, /readonly recipientSummaryError = signal<string \| null>\(null\)/);
 });
+
+test('monthly recipient summary explicitly reports gross confirmations before returns', () => {
+  assert.match(tbankSource, /Подтверждённые оплаты по получателям до возвратов/);
+  assert.match(tbankSource, /Подтверждено до возвратов/);
+  assert.match(webTbankTemplate, /Подтверждённые ручные оплаты · до возвратов/);
+  assert.match(webTbankTemplate, /последующие возвраты здесь не вычитаются/);
+  assert.match(webTbankTemplate, /Подтверждено до возвратов/);
+});
*** End Patch
