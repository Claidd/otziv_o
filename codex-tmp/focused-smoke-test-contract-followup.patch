*** Begin Patch
*** Update File: backend/src/test/java/com/hunt/otziv/payments/PaymentLinkServiceTest.java
@@
-        ordered.verify(paymentLinkRepository).save(any(PaymentLink.class));
+        ordered.verify(paymentLinkRepository, times(2)).save(any(PaymentLink.class));
         verify(orderRepository, never()).findByIdForMutation(410L);
*** Update File: backend/src/test/java/com/hunt/otziv/payments/repository/PaymentLinkArchiveMySqlIntegrationTest.java
@@
                     bank_init_nonce VARCHAR(64) NULL,
                     bank_cancel_nonce VARCHAR(64) NULL,
                     bank_cancel_origin_status VARCHAR(32) NULL,
+                    last_error VARCHAR(1000) NULL,
+                    manual_actual_recipient_frozen_at DATETIME(6) NULL,
                     payment_success_notified_at DATETIME(6) NULL,
*** End Patch
