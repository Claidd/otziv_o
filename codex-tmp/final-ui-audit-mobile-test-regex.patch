*** Begin Patch
*** Update File: mobile/test/tbank-payment-task-safety.test.mjs
@@
-  const createMethod = tbankSource.match(/async createManualTask\(\): Promise<void> \{[\s\S]*?\n  \}\n\n  resetAdminTaskDraft/);
+  const createMethod = tbankSource.match(/async createManualTask\(\): Promise<void> \{[\s\S]*?\r?\n  \}\r?\n\r?\n  resetAdminTaskDraft/);
@@
-  const method = tbankSource.match(/async confirmManual\(link: AdminPaymentLinkResponse\): Promise<void> \{[\s\S]*?\n  \}\n\n  async markManualReceipt/);
+  const method = tbankSource.match(/async confirmManual\(link: AdminPaymentLinkResponse\): Promise<void> \{[\s\S]*?\r?\n  \}\r?\n\r?\n  async markManualReceipt/);
@@
-  const method = tbankSource.match(/async load\(\): Promise<void> \{[\s\S]*?\n  \}\n\n  setMode/);
+  const method = tbankSource.match(/async load\(\): Promise<void> \{[\s\S]*?\r?\n  \}\r?\n\r?\n  setMode/);
*** End Patch
