# MySQL 9.0 → 9.7.3 local upgrade rehearsal

This opt-in harness reads **only** the sanitized `otziv-prod-local-mysql-1` source, then works on its own Docker network, named data volume and fixture accounts. It does not change Compose, the production pin, the source database, application code or the separate rollback fixture. No ports are published; the network is `--internal`.

The [official upgrade matrix](https://dev.mysql.com/doc/refman/9.7/en/upgrade-paths.html) supports an Innovation release to its next LTS by an in-place upgrade. MySQL 9.0 → 9.7 is that path. The [9.7.3 release note](https://dev.mysql.com/doc/relnotes/mysql/9.7/en/news-9-7-3.html) identifies it as the Docker-only Critical Security Patch Update. The candidate is pinned to `container-registry.oracle.com/mysql/community-server@sha256:bf955135c21e2c1c153417407fbbbb8efa786f487781b1fb6baaf7aba17d4340`, with amd64 image configuration `sha256:3fa754b144aeb7be887e6c614522aa06a2bbcd8ac2f3b67414884fa79000cdab`. Its actual binaries report mysqld **9.7.3** and MySQL Shell **26.7.1**; a tag alone is not accepted as proof.

## Run

Use local Docker and Node 22+. Prevent source DDL until `DUMP_CAPTURED`; ordinary transactional DML is compatible with the single-transaction capture. The operator must finish the approved offline migration/performer maintenance before declaring `SOURCE_READY`. The first captured rehearsal source was schema **1.10.300**; the independent hardened-image follow-up captures **1.10.306**. Do not pass this flag while source maintenance or another migration is running.

```powershell
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs capture E:/private/mysql-upgrade-run --source-ready
# Source may resume only after DUMP_CAPTURED; subsequent work never reconnects to it.
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs rehearse E:/private/mysql-upgrade-run --app-env-file E:/private/prod-local.env --app-image otziv-app:finalization-shutdown-proof
node --test infrastructure/runtime-security/mysql-upgrade-rehearsal.test.mjs
```

For the reviewed missing-definer fixture and additional hardened-runtime proof, use a **new capture directory**, then append `--prepare-reviewed-view-definer --hardened-image otziv-mysql:9.7.3-proof --retain-after-pass` to the rehearsal command. This opt-in definer preparation uses the same exact metadata/locked SELECT-only checks described below before the first checker, without recreating the already diagnosed original failure. The hardened tag resolves to an immutable local image ID recorded before execution. The upstream image remains the separate full Upgrade Checker artifact; the hardened runtime can omit MySQL Shell while retaining the server and client binaries.

The capture directory must not exist. On Windows its inherited ACL is removed and only the current user SID and SYSTEM receive access; on Unix its mode is 0700. `mysqldump --single-transaction --quick` streams binary bytes directly through gzip to a private file, including routines, triggers, events and hex blobs. No PowerShell text transcoding, credentials or user rows enter stdout. The capture records size/SHA256, source image/version and the Flyway-history hash. It is a local sanitized application-schema dump, **not** a replacement for the paired recovery archive.

The second command is exclusive and will refuse an existing `rehearsal.claim`. It creates a 1.5 GiB/2 CPU MySQL 9.0 clone and restores the captured bytes. The real application image migrates the 1.10.300 capture to 1.10.306, or validates an already-1.10.306 capture, passes authenticated database startup/Flyway/Hibernate validation and reaches `/actuator/health`. The app has a 2 GiB/2 CPU limit. Local env-file credentials never enter process arguments or evidence; database credentials are replaced with ephemeral fixture values. External send/registration/dispatch/backup flags are disabled, independently of the internal network fence. This health proof does not test live Keycloak authentication.

There is one explicit fixture-preparation recovery option, `--resume-app-preparation`. It is accepted only for an app-startup failure on the still-9.0 clone **before any checker or target-server attempt**. It preserves the failed result, verifies the unchanged app image/configuration, stopped resources, ownership labels and internal network, and starts the same existing 9.0 volume. It does not reimport or replace data. This exists to correct missing Compose-provided disabled integration placeholders when using a raw local env file. It cannot resume or bypass a failed Upgrade Checker or failed 9.7 upgrade.

The observed application image originally used `sh -c java ...`, which left Java behind a shell PID1. Its real `docker stop --time 120` ended with forced exit137 while health remained UP. That failure is retained. The corrected image `sha256:0b5307c31fc60e744bebf22b65495a7111bb775b066fcf3517d918fe874ae470` changes only the entrypoint to `exec java`; the resume guard verifies identical RootFS and every other image configuration field. Java PID1 may exit143 after SIGTERM: accept that only with no OOM/forced kill and completed graceful shutdown before the deadline. MySQL must exit0. Do not use an exit-code wrapper to conceal forced termination.

One separately reviewed correction is available through `--resume-view-definer-preparation`. The application-schema dump omits MySQL system users. On the observed clone, two unchanged `SQL SECURITY DEFINER` views (`analytics_payment_source` and `analytics_salary_source`) referred to the missing `hunt@%` database account; `CHECK TABLE` labelled those views `Corrupt`. The original full checker RED and direct diagnostic output are retained. This option accepts only those exact two errors before any target server has started, checks the actual view metadata and absence of that account, and creates a **locked** synthetic definer with **SELECT only** on the clone schema. No production password or login is introduced; view definitions are hashed before/after and must remain unchanged. The entire mandatory checker then runs again without exclusions. This is a correction of the incomplete fixture account inventory, not a general repair or a means to ignore actual corruption. Any other checker or target-upgrade failure still stops the run.

The app is stopped before the checker or comparison snapshots. The [official Upgrade Checker](https://dev.mysql.com/doc/mysql-shell/9.7/en/mysql-shell-utilities-upgrade.html) runs against the still-running 9.0 clone, using the candidate's bundled Shell and explicit target `9.7.3`, with the clone's complete option file. There are no check exclusions. A nonzero error count, empty/incomplete checks, wrong version, parser failure or failed process stops the run **before** the target server opens the volume. Warnings and manual checks remain evidence requiring review; the harness does not redefine them as zero risk.

After a successful checker, `innodb_fast_shutdown=0` and a clean MySQL exit are mandatory. Only then is the **same named volume** attached to the exact 9.7.3 image. The Oracle image uses UID27 by default, while the source image uses UID999. The harness explicitly retains UID999 using Oracle's non-root entrypoint support; a dedicated UID999 tmpfs supplies writable runtime files. It does not recursively rewrite database ownership or silently initialize another data directory. The generated option file retains existing nonstandard-FK compatibility, keeps database events off for the fixture, and is hashed in the result.

## Evidence and failure behavior

`rehearsal.json` records exact image identities, actual server version, checker outcomes and assertions. Private companion files contain:

- all application base-table row counts and the exact Flyway version/checksum history;
- extended data checksums for selected business and durable-queue tables;
- synthetic JSON-function results and counts/validity/aggregate CRC checks for every actual JSON column;
- native lead/performer queue selection SQL, its hash and successful `FOR UPDATE SKIP LOCKED` execution followed by rollback;
- app health, protected startup/server logs and resource limits.

Before/after row counts, history, representative checksums and JSON results must match before the target app starts. The real app then starts on 9.7.3 and passes health again. Queue probes verify SQL compatibility; they are neither a delivery attempt nor a throughput benchmark. Source MySQL users/grants are deliberately not copied, so this run proves the current JDBC client with isolated caching-SHA2 fixture accounts, not every production account policy.

With `--hardened-image`, an exclusive `upstream-rehearsal.json` freezes the upstream result before the additional phase. After a clean upstream stop, the hardened server opens the **same volume**, repeats the full integrity comparison and app health, and then undergoes a clean stop/start with another full integrity comparison. The app must run as Java PID1, finish before the shutdown deadline, and log completed Spring graceful shutdown, JPA closure and completion of every observed Hikari pool shutdown. Exit143 alone is insufficient. The new follow-up never rewrites the original upstream-only result directory.

Any checker/upgrade failure is retained with the phase and stopped owned resources. There is no `--upgrade=FORCE`, ignored error, grant bypass, new-volume fallback or automatic downgrade. Successful cleanup checks exact ownership labels and paths, then removes only this run's containers/network/volume and its protected dump/fixture credential file. Original failure evidence is not rewritten as a successful run.

`--retain-after-pass` deliberately leaves the owned containers stopped and retains the volume, network, protected dump and fixture credentials for an agreed follow-up. They remain private and are listed in the result. Subsequent cleanup requires checking those exact ownership labels and resolved paths; this flag does not grant access to unrelated fixtures.

There is **no supported binary downgrade of this 9.7 data directory to 9.0**. The [official downgrade matrix](https://dev.mysql.com/doc/refman/9.7/en/downgrading.html) does not provide in-place LTS→earlier Innovation rollback. Keep a verified pre-upgrade backup paired with its app/configuration/secrets and restore that pair when rollback is required. This rehearsal's disposable dump is deleted on success and is not that operational backup. Production activation still requires a fresh backup/restore decision, actual account/configuration review and the candidate's separate vulnerability scan; this script grants no release approval.

## Explicit C7 published-image rollback mode

The commands above remain the default upstream-image rehearsal, including their existing restricted preparation/resume and optional hardened-image paths. `--published-rollback` selects an additional, separately bound mode in this same harness; it does not change any database default or deployment gate.

```powershell
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs capture E:/private/mysql-published-rollback-run --source-ready --published-rollback
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs rehearse E:/private/mysql-published-rollback-run --published-rollback --app-env-file E:/private/prod-local.env --app-image sha256:eb5419e630ed6bd3f21c9c510c3961797b7faf5e6a81ed400e0ba4ebd7d38a0f
```

This mode accepts only C7's published MySQL manifest `ghcr.io/claidd/otziv-security@sha256:3a3caaab4e71b3bfdec9da21c17c00ed10ca237151919aeddac8e5ce4b8b7baa`, Linux/amd64, and configuration `sha256:80ee3b50147a329addbaf754abc4dce86dc06636624680d780351e2320a11070`. The existing upstream Oracle image remains the complete Upgrade Checker provider. Both identities are recorded separately. The example application identity is the reviewed local fixture release; select a separately reviewed app/configuration pair for a future rehearsal rather than assuming the example certifies another release.

Capture still requires a new protected directory and the source DDL fence. It also checks the expected local Compose MySQL project/service/volume, records the configured application's account name and exact grants, and requires identical Flyway history before/after the logical dump. Only the reviewed account shape is accepted: non-root application user at `%`, global `USAGE`, and `ALL PRIVILEGES` on the captured database only. The clone receives a newly generated password; the original password and other source users are not exported. Unsupported account policies stop the run for review.

The new capture schema is `otziv-mysql-published-rollback-capture-v1`. Both `capture.json` and `owner.json` bind `mode: published-rollback`, the exact target identities and an owner with the `otziv-mysql-c7-rollback-` prefix. Rehearsal requires the explicit flag and all bindings to agree before reading the dump, writing a run claim or contacting Docker. A generic V1 capture cannot be promoted; a published-mode capture cannot silently take the generic route. The earlier private C7 prototype's V1 capture was not mode-bound and is historical evidence only: create a fresh capture rather than editing its metadata. Published mode refuses preparation/resume, `--hardened-image`, `--retain-after-pass`, unknown or duplicate flags and missing flag values.

The configured account/grants are recreated and tested on the initial 9.0 clone. After the actual app is healthy and stops gracefully, a synthetic before-upgrade marker is included in a new hash-verified logical backup. The full existing Upgrade Checker must pass before the exact C7 runtime opens the same data volume as UID999. All previous data/history/JSON comparisons and app health/shutdown checks remain. A second marker is then written and must survive a candidate restart.

Rollback restores the pre-upgrade backup to a separate, initially empty 9.0 volume. It checks the reproduced application account/grants, complete recorded snapshot comparisons, presence of the before-upgrade marker and absence of the post-upgrade marker. The same application image must become healthy on the restored database and finish Spring/JPA/Hikari shutdown within the existing deadline. The upgraded data volume is never opened with an older binary.

Successful cleanup removes only this owner's containers/network/two volumes and the exact `source.sql.gz`, `preupgrade.sql.gz` and `clone-secret.json` files after path/ownership checks. Failure preserves the stopped owned resources and protected evidence. There is no automatic retry, grant widening, checker exclusion, binary downgrade or operational recovery-point retention. Database migration continuity, future release approval, production configuration/account review and image security acceptance remain separate requirements.

## C14 current-release rehearsal and configuration binding

C12's 49 runtime assertions and 33 unit tests are historical evidence for schema **1.10.306**. The independent C14 invocation uses a new source capture at **1.10.310**, application image `sha256:f6123a92f20df6125647bdbe181afb50c6bbc14c595e547f1ce3649db5ccd213`, and the same immutable published MySQL candidate above. Its actual result, source binding, security report and retained failed configuration attempt are in [`proofs/c14-mysql`](proofs/c14-mysql). Read the result status and hashes; the existence of this section does not turn a pending or failed invocation into PASS.

Published-mode capture now binds the source's actual `character_set_server`, `collation_server`, `lower_case_table_names`, `restrict_fk_on_non_standard_key`, `sql_mode` and `time_zone`. Unsupported policies stop for review. These six settings are read back and compared on the initial 9.0 clone, upgraded 9.7.3 server and fresh 9.0 rollback server. Database events remain deliberately OFF, and memory/connections are bounded fixture settings. This is an explicit compatibility subset, **not** equivalence of every server variable. The current source uses `utf8mb4_unicode_ci`, `+08:00`, case policy 0 and nonstandard-FK compatibility OFF. Published mode validates its captured supported schema (1.10.306 or 1.10.310); it does not silently migrate a future schema or accept edited capture metadata. Old captures lacking the semantic configuration binding must be recaptured.

The first C14 attempt failed before import/checker/upgrade because Docker Desktop exposed the Windows bind-mounted option file as world-writable. MySQL explicitly ignored that file; actual collation/time zone/FK values differed from its declared contents. The failed proof is retained. Published-mode MySQL containers now copy those exact bytes to a native container file with mode 0644 and execute the image's original entrypoint with that file. The existing UID999 and owned-volume policy is preserved. A causal unit check covers the wrapper and rejection of alternate server commands; the real read-back assertions prove that the server used the settings. No failed run is resumed or rewritten. Historical C12 results did not include these actual-setting assertions and cannot establish them retrospectively.

The current snapshot also checks the presence and extended checksums of `order_publication_client_updates`, `order_client_message_occurrences`, `client_message_operations` and `scheduled_client_message_state`, in addition to all base-table counts and complete Flyway history. The new publication worker is held by its initial-delay setting alongside the existing external-effect fences. Queue selection probes still roll back; they do not perform deliveries. Source capture is read-only and transactional; source DDL is fenced until `DUMP_CAPTURED`.

```powershell
node --test infrastructure/runtime-security/mysql-upgrade-rehearsal.test.mjs
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs capture E:/private/mysql-c14-run --source-ready --published-rollback
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs rehearse E:/private/mysql-c14-run --published-rollback --app-env-file E:/private/current-local-app.env --app-image sha256:f6123a92f20df6125647bdbe181afb50c6bbc14c595e547f1ce3649db5ccd213
node infrastructure/runtime-security/scan.mjs image ghcr.io/claidd/otziv-security@sha256:3a3caaab4e71b3bfdec9da21c17c00ed10ca237151919aeddac8e5ce4b8b7baa E:/private/c14-vulnerabilities.json
```

The Upgrade Checker reports warnings separately from errors. For C14 its 24 warnings concern server-variable deprecation or changed defaults; there are no manual checks. In particular, source `gtid_mode=OFF` and MySQL 9.7's changed GTID/enforcement defaults require a production replication-policy decision. FK compatibility and `log_bin_trust_function_creators` deprecation also remain recorded. A successful local upgrade is not a decision to adopt every new default, nor a performance/replication test.

The separately recorded C14 standalone configuration probe starts the exact target on a fresh owned UID999 volume with explicit `--gtid-mode=OFF --enforce-gtid-consistency=OFF --log-bin=mysql-bin --binlog-format=ROW --event-scheduler=OFF`. It reads those actual settings, performs a synthetic JSON/operation-identity write and proves both settings and data survive a clean restart. This is an additional configuration probe, not another copy of the full upgrade/rollback assertion set. Its fresh initialization uses numeric `user=999`, an empty volume with Docker copy-up disabled and UID999 writable runtime directories; it does not alter ownership of an existing database. `binlog_format` remains a deprecation warning. The read-only VPS inventory supplied to release review has GTID/enforcement OFF, binlog ON/ROW and no replica rows, so coordinated activation must preserve this explicit standalone policy. Keep events OFF while quiescent and restore the reviewed original ON policy only on controlled resume.

## Coordinated VPS activation inputs and order (not executed by C14)

The ordinary deployment image-identity guard must continue to reject a 9.0→9.7 image change on an existing volume. `DATABASE_HOLD` remains fail-closed without a reviewed, hash-bound transition proof. C14 is preparation evidence for a separate coordinated procedure; it is not a bypass token and does not authorize an ordinary `compose up` to upgrade the VPS.

Before scheduling that procedure, the release owner must bind these concrete inputs in the activation record:

- the release commit, tested application image/configuration pair, old MySQL image `sha256:8b879a3959bc59adcb7281a41950d39cf8c9b3fb23b87b9b62318ce884a7c383`, exact published target manifest/configuration digests above, Linux/amd64, and the **actual source Flyway 1.10.287 / candidate 1.10.310** history/checksums. The earlier local310 rehearsal does not cover this application migration;
- the exact VPS host, Compose project/service, current container ID, mounted source volume ID, ownership/UID policy, native option-file SHA256/mode and all effective options, including actual users/grants/definers, plugins, events, GTID/binlog/replication policy and the retained checker warnings; the local capture reproduces only the configured application account, not the VPS account inventory;
- an approved downtime/write-fence owner, current write sources and automation/self-heal timers, release checks, stop criteria, rollback RPO/RTO and the operator responsible for reconciliation of any post-upgrade writes;
- a **fresh** verified pre-upgrade recovery point, its encrypted artifact/manifest hashes, independent storage/version receipt, separately held decryption key and tested restore to an empty 9.0 volume, paired with the previous application/env/secrets and required Keycloak, integration-state and object-store recovery points; the deleted C14 disposable dump is not this backup;
- the exact new empty rollback volume/container identities and old app/configuration recovery pair, plus the hash-bound C14 result, runtime/checker/security reports and helper source binding. Any changed image, schema, account/configuration policy or source topology requires renewed review/proof.

Execute only the approved coordinated procedure, recording each check:

1. Acquire the existing deployment lock, suspend self-heal/restart automation, close ingress and stop **all** application/background/external writers. Verify active DB sessions and absence of DDL. Explicitly fence database events and record their original policy. Keep secrets out of command arguments and logs.
2. Create and verify the fresh encrypted backup with `infrastructure/scripts/prod/create-pre-deploy-db-backup.sh` and the paired recovery workflow in [`../recovery/README.md`](../recovery/README.md). Confirm its independent receipt and restore it to the identified **empty disposable** 9.0 volume. Validate schema, representative data, grants and the paired app there before altering the source volume. A restore command must never target the live source as a rehearsal. Retain the encrypted operational recovery point after the drill.
3. Run the complete official Upgrade Checker against the quiescent 9.0 server using the reviewed target and exact effective native option file. Review every warning and all actual VPS accounts/configuration; no exclusion or FORCE. A world-writable/ignored option file, mismatched settings, errors or unexpected checks stop activation.
4. Confirm completed Spring/JPA/Hikari shutdown; set `innodb_fast_shutdown=0` and require a clean MySQL exit0. Record the source volume identity. The reviewed coordinated mechanism may now attach **only** that volume to the exact new image with verified UID/configuration. It must not delegate this transition to ordinary deployment or weaken the identity guard.
5. Before reopening writers, verify actual 9.7.3, required effective settings/grants, unchanged Flyway history, base-table counts, business/delivery identity checksums and UNKNOWN records, startup/health and a contained app read/write canary. Rehearse or confirm the rollback decision point, then let the release owner release the write fence and restore approved event/automation policies. Live Keycloak/integration checks and production monitoring remain part of this activation.
6. If rollback is chosen, stop/fence the new writers and preserve the upgraded volume for investigation. Restore the verified **pre-upgrade** pair into the predesignated **fresh 9.0 volume**, validate the backup hashes, data/Flyway/identity records, grants and old app health, and only then switch service routing. The old binary must never open the upgraded volume. Restoring the pre-upgrade snapshot excludes later writes; the owner must explicitly reconcile/accept that recovery point before service resumes. C14 proves this distinction using separate before/after markers; it cannot promise lossless rollback after arbitrary production writes.

## Actual VPS287 application-pair replay

The explicit `--vps-release-upgrade` branch imports a separately captured, read-only VPS snapshot. It never connects to the VPS. The private input directory must be outside the repository and contain the original `capture.json`, `source.sql.gz`, complete Flyway metadata, exact account/configuration companions, private old app env, and `old-app-copy.json`. `import-vps` authenticates the supplied hashes, exact project/container/volume and source287, then copies into a new protected owner directory. It cannot consume the earlier local310 capture or resume a failed invocation.

```powershell
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs import-vps E:/private/mysql-vps-run --input-dir E:/private/actual-vps-capture
node infrastructure/runtime-security/mysql-upgrade-rehearsal.mjs rehearse E:/private/mysql-vps-run --published-rollback --vps-release-upgrade --baseline-app-image sha256:bcd87bdb514c6477b95b4f2f04abe84e9d207339aa5ca608f35628e739c5c7e3 --baseline-app-copy-receipt E:/private/actual-vps-capture/old-app-copy.json --app-image sha256:f6123a92f20df6125647bdbe181afb50c6bbc14c595e547f1ce3649db5ccd213 --app-env-file E:/private/actual-vps-capture/old-app.env
node --test infrastructure/runtime-security/mysql-upgrade-rehearsal.test.mjs infrastructure/runtime-security/mysql-vps-release-rehearsal.test.mjs
```

The old VPS image has no repository digest. Its exact configuration is `sha256:dd0585b6b55e4159a927683386b557fa38c3bd590c1c262694a52d94818a00ba`. A read-only export/load into Docker Desktop's containerd store produces manifest ID `sha256:bcd87bdb514c6477b95b4f2f04abe84e9d207339aa5ca608f35628e739c5c7e3`; the receipt proves a local re-export contains the exact original configuration bytes and all fourteen RootFS layers, Linux/amd64. The branch verifies this explicit pair and the runtime RootFS. A matching tag, self-declared image label or arbitrary manifest/config substitution is insufficient.

The source account is exactly `hunt@%`, with `USAGE`, database-level grants on `otziv` and its two named QA schemas `otziv_archive_check_20260714` / `otziv_migration_check_20260714`. Only these four captured grants are admitted; QA data is not copied and the clone uses a new random password. All three database phases explicitly use GTID/enforcement OFF, binlog ON/ROW and events OFF, in addition to the six semantic settings. Fresh, authenticated SQL verifies those effective values.

The sequence starts the exact old app against287, stops it and requires the captured business/count/history/JSON snapshot unchanged. Both original-app validation phases use actual MySQL `read_only=ON` and `super_read_only=ON`, verified by SQL; after graceful stop both flags are verified OFF before later writable phases. This demonstrates old app health, Hibernate/Flyway validation and graceful shutdown; it does not claim a writable old-app business API test. The explicit server fence is needed because the captured old app's immediate contractor shadow backfill has no independent scheduling-disable switch: an earlier failed attempt added22 `contractor_shadow_backfill_claims` while all eight required business checksums remained unchanged. That is recorded background work, not corruption or permission to omit a table from comparisons. A bounded diagnostic proved the server fence blocks the claims while old-app health and complete shutdown still pass.

After full checker acceptance and clean shutdown, the same data volume opens with9.7.3; the engine-only snapshot must still match287. Candidate f612 then performs the real append-only migration287→310 and becomes healthy with the database writable. Existing UNKNOWN/LEGACY_UNKNOWN rows retain all original columns and delivery envelopes byte-for-byte; migration-added columns are not confused with changes to original data. Source287 queue probes use the actual `lead_sync_queue` and performer assignment structures; source business tables are mandatory. Target310 additionally requires all new delivery identity tables. A clean candidate restart and synthetic write distinguish persistence from an empty startup. Rollback restores the pre-upgrade287 backup to a different fresh9.0 volume, then the exact old app becomes healthy under the stated read-only validation fence and preserves the restored snapshot.

The real old app uses `sh -c java` without `exec`. Its container PID1 is a shell; this is not claimed as working stock Docker graceful shutdown. For this exact copied image only, the helper verifies a single direct Java child, its parent PID, process name and executable, repeats the identity checks immediately before TERM, signals that JVM, and requires the shell/container to exit0/143 with complete Spring/JPA/Hikari logs and no OOM/forced kill. All other apps retain the Java-PID1 rule. The production coordinated procedure must account for this old-image behavior before backing up; it must not silently rebuild the old app to hide the risk.

The isolated app has an internal-only network, no host ports and a read-only root. Scheduled backup, catch-up and one-shot backup flags are all OFF; its required writable backup directory is in `/tmp`. The real VPS backup configuration remains unchanged. Early failed attempts are retained, including old-schema table assumptions and inconsistent fixture backup settings. A later successful run is a new complete capture/import invocation, not a rewritten failure.

Only [`proofs/c14-mysql-vps/actual-result.json`](proofs/c14-mysql-vps/actual-result.json), once present with actual PASS and verified hashes, can attest this specific replay. The dedicated validator returns `COORDINATED_CANDIDATE_PREPARATION`, `vpsCutoverExecuted:false` and `ordinaryDeploymentUpgradeAuthorized:false`. It supplements the complete original C7 publication/anonymous pair; it does not replace it. Actual VPS activation still requires a fresh encrypted backup with an independent copy and empty-volume restore, the current source identity/configuration checks, a reviewed cutover plan, and a rollback decision before writers resume.
