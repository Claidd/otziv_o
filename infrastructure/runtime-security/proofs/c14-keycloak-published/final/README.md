This is the independent security and byte-binding proof for published Keycloak `ghcr.io/claidd/otziv-security@sha256:c6ee6afd6750ae0886a608c5837085cb20417a6746ca2ed47b59b1a0fdec1973`, config `sha256:89f560e111619be776f984e2f317a775cb37ce20660271dbb25f638b454ffeab`.

GitHub workflow run 34249337258, attempt 1, source commit `ca1dcaa0b0f8c063f8e541c1623a3f61d68e749e` completed successfully. Publication job 102139529463 and fresh anonymous-download job 102140211171 both passed. The original ZIP archives are retained and independently matched to GitHub API SHA256 digests: publication artifact 10065404002 and anonymous artifact 10065516474. Artifact names include the attempt number; no prior artifact was overwritten. Registry index, amd64 manifest/config, provenance and SBOM descriptor hashes, exact source commit/Dockerfile, and the publication/anonymous pair were revalidated using the repository's existing registry primitives.

The fresh full hosted Trivy report contains **0 HIGH, 0 CRITICAL and 49 lower findings**. All 143 Ubuntu packages and 512 Java package records remain in the raw report; the CycloneDX SBOM contains 656 components. Existing Jackson 2.21.6/Parsson 1.1.9 policy independently passes on all 17 recognized records. Raw SHA256 is `731bc286c53dd265d34b8f7d09056f03759c47a095b04a0a5f2566be9d6baad4`. No new CVE exclusion or scan-policy change was used.

An independently pulled image was checked against the registry OCI config and exported from a stopped, never-executed, isolated container. The complete root filesystem was compared with the already reviewed local v3 inventory. Contents, file types, permissions, uid/gid and symlink targets were checked; mtime and Docker's generated hosts/hostname/resolv.conf files were excluded. Of 476 Keycloak JAR files, 474 are byte-identical. Both patched vendor modules, all other dependencies, the custom provider, CLI binaries, OS files and actual source-provenance sidecar are unchanged. Only generated-bytecode.jar and transformed-bytecode.jar differ: the former has 136 differing uncompressed entries, while the latter differs only in ZIP packaging. Launch configuration is unchanged except for expected publication labels.

Because generated bytecode differs, this proof does **not** transfer local-v3 runtime results to the publication. The independent database migration/rollback and issuer protocol acceptance is produced separately against this exact published image/config. `activation-candidate.json` is only the publication/anonymous evidence pair; it is intentionally not a complete activation authorization. The final repository activation validator additionally requires the separate published runtime acceptance. The local `verify-publication-pair.mjs` checks the source/registry/download evidence needed to construct that acceptance; it does not replace or relax the final deployment gate.

`review.json` freezes every file in this directory and all nine source build inputs. Executed helper scripts are preserved as text evidence; the private Docker rootfs export remains in scratch. The rootfs inventory and comparison are bound explicitly by their SHA256 hashes. No running application, database, volume or VPS was changed by this verification.

Run the bounded offline integrity checks with:

```
node --test infrastructure/runtime-security/proofs/c14-keycloak-published/final/published-security.test.mjs
```

The earlier run 34247828233 and image 949f are retained in the parent directory as unsuccessful publication history: its anonymous job failed at GitHub artifact discovery with HTTP 403 before verification. That history is not used as successful anonymous-download evidence for this image.
