import assert from 'node:assert/strict';
import {readFile,writeFile,mkdir} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import {resolve} from 'node:path';
import {run} from '../../infrastructure/recovery/process.mjs';
import {createEvidenceReader,validateActivation} from '../../infrastructure/runtime-security/reviewed-image-defaults.mjs';
import {adjudicatePostgresImage,loadPostgresProof} from '../../infrastructure/runtime-security/postgres-c14-adjudication.mjs';
import {summarizeReport,TRIVY_IMAGE} from '../../infrastructure/runtime-security/scan.mjs';
import {combinedScanSummary} from '../../infrastructure/runtime-security/scan-verdict.mjs';
const root=process.cwd(),p='infrastructure/runtime-security/proofs/c14-postgres-published/',scratch=resolve('.codex-tmp/release-readiness-20260908/postgres-published-runtime');
await mkdir(scratch,{recursive:true});
const hash=b=>createHash('sha256').update(b).digest('hex'),json=async f=>JSON.parse(await readFile(p+f));
const entry=await json('publication-candidate.json'),publication=await json('publication/publication.json'),manifest=await readFile('infrastructure/runtime-security/reviewed-images.json');
const original=JSON.parse(manifest).images.find(i=>i.component==='postgres');
const actualReference=await validateActivation(original,entry,manifest,await createEvidenceReader(root));assert.equal(actualReference,publication.reference);
console.log('PASS existing_full_publication_anonymous_OCI_manifest_validation');
const rawBytes=await readFile(p+'publication/vulnerabilities.json'),report=JSON.parse(rawBytes),saved=await json('publication/vulnerabilities.adjudications.json');
assert.equal(hash(rawBytes),saved.rawReportSha256);assert.equal(saved.rawReportModified,false);assert.equal(saved.imageConfigId,publication.imageId);assert.equal(report.Metadata.ImageID,publication.imageId);
assert.equal(saved.postgres.status,'EXACT_RUNTIME_VERIFIED');assert.equal(saved.postgres.rawReportSha256,hash(rawBytes));assert.equal(saved.postgres.rawReportModified,false);
const actual=JSON.parse(await run('docker',['image','inspect',publication.reference]))[0],config=await json('publication/registry-amd64-config.json');
assert.ok([publication.imageId,publication.reference.split('@')[1]].includes(actual.Id));assert.equal(actual.Os,'linux');assert.equal(actual.Architecture,'amd64');assert.deepEqual(actual.RootFS.Layers,config.rootfs.diff_ids);
for(const key of ['Env','Entrypoint','Cmd','WorkingDir','User','ExposedPorts','Volumes','Labels'])assert.deepEqual(actual.Config[key]??null,config.config[key]??null,key);
console.log('PASS actual_pulled_image_exact_OCI_rootfs_and_configuration');
const {review,reviewSha256}=await loadPostgresProof();
const repeated=await adjudicatePostgresImage(report,rawBytes,actual.Id,scratch);assert.equal(repeated.status,'EXACT_RUNTIME_VERIFIED',repeated.reason);
const normalized=x=>{const {immutableImageId,...rest}=x;return rest;};assert.deepEqual(normalized(repeated),normalized(saved.postgres));
const summary={...combinedScanSummary(summarizeReport(report),{grafana:saved,alloy:saved.alloy,postgres:repeated}),scannerImage:TRIVY_IMAGE};assert.deepEqual(summary,publication.security);
const findings=report.Results.flatMap(r=>r.Vulnerabilities??[]);assert.equal(summary.high,26);assert.equal(summary.critical,1);assert.equal(summary.effectiveBlockingFixedHighOrCritical,0);assert.equal(summary.effectiveUnfixedHighOrCritical,0);
console.log('PASS repeated_stopped_export_exact_runtime_source_payload_and_all_reviewed_adjudications');
for(const receipt of await json('artifact-verification.json')){assert.equal(receipt.commit,entry.commit);assert.equal(receipt.run,entry.run);assert.equal(receipt.attempt,'1');assert.equal(receipt.verified,true);assert.equal(receipt.apiDigest,receipt.actualDigest);assert.equal('sha256:'+hash(await readFile(p+receipt.name+'.zip')),receipt.apiDigest);}
const result={schema:'otziv-published-postgres-image-verification-v1',result:'PASS',timestamp:new Date().toISOString(),reference:actualReference,imageConfigDigest:publication.imageId,actualLocalId:actual.Id,rootfsMatchesRegistryConfig:true,imageLaunchConfigurationMatchesRegistry:true,publicationAndAnonymousFullValidation:true,versionedManifestValidated:true,sourceReviewSha256:reviewSha256,reviewProofFilesValidated:Object.keys(review.files).length,reviewedPayloadFiles:repeated.verifiedPayloadFiles,runtimeIdentitySha256:repeated.runtimeIdentitySha256,independentlyReviewedRuntimePayloadIdentical:true,inspectedBinaryExecuted:false,rawReportSha256:hash(rawBytes),rawFindings:findings.length,rawHigh:summary.high,rawCritical:summary.critical,adjudicatedDecisions:repeated.decisions.length,adjudicatedFixedHighOrCritical:summary.adjudicatedPostgresFixedHighOrCritical,adjudicatedUnfixedHighOrCritical:summary.adjudicatedPostgresUnfixedHighOrCritical,effectiveFixedHighOrCritical:0,effectiveUnfixedHighOrCritical:0,rawReportModified:false,sourceFiles:Object.fromEntries(await Promise.all(['infrastructure/runtime-security/postgres-c14-adjudication.mjs','infrastructure/runtime-security/reviewed-images-c14-postgres.json','infrastructure/runtime-security/builds/postgres-c14/Dockerfile'].map(async f=>[f,hash(await readFile(f))]))),productionChanged:false,defaultsChanged:false,activationApplied:false,vpsMigrationReadinessClaimed:false};
await writeFile(p+'published-image-verification.json',JSON.stringify(result,null,2)+'\n',{flag:'wx'});await writeFile(p+'repeated-runtime-adjudication.json',JSON.stringify(repeated,null,2)+'\n',{flag:'wx'});console.log(JSON.stringify(result));
