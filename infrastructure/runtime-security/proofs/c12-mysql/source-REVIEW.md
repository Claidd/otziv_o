# Independent source review: additive MySQL and exact Alloy decision

**PASS. No blocking defect found in the reviewed candidate bytes.** Seventy-three offline integrity checks passed; this is an independent source/evidence review, not another execution of the database fixture or scanner.

## MySQL additive mode

Exactly three integration paths match `mysql-c7-integration/integration-manifest.json`: the generic rehearsal, its tests, and an appended runbook section. Current publication preimages and proposed postimages were independently hashed; the two-file patch was regenerated in memory and matches the retained patch. The runbook retains the complete previous bytes as a prefix. Recovery process/environment helpers remain unchanged.

The new mode requires explicit `--published-rollback`, matching V1 published capture/owner metadata, the exact C7 image/configuration pair and a newly generated owner. Unsupported or mixed legacy flags reject before dump/claim/Docker access. Legacy capture/resume/preparation/hardened paths remain separate. New account SQL accepts only a bounded identifier and exactly the captured database-wide grant plus global USAGE; no source password is copied. The candidate is executed by immutable image reference with UID999 and the dedicated writable tmpfs. The complete official checker still precedes same-volume upgrade.

Backup is taken with the fixture application stopped. The new post-upgrade canary is distinguished from the pre-upgrade backup. Restore uses a newly allocated old-version volume, validates that it started empty, replays the reviewed clone account, and compares snapshots before restarting the same application image. It never attaches the old server to the upgraded volume. Cleanup verifies owner labels and deletes only exact owned output files; failure retains stopped resources for diagnosis rather than silently falling back.

The retained new CLI execution independently binds its before/after candidate hashes through `source-binding.json` and `run-proof.py`, with 49 successful runtime checks, all seven checker categories complete, no manual checker items, unchanged source containers/images/mounts, and verified cleanup. The 33-test log is retained and hash-checked. The earlier 49-check prototype is preserved separately; the new run is not a relabelled old proof. Legacy real-volume tests were not repeated, and the review does not claim otherwise. This integration does not activate a database default or authorize production rollout.

## Alloy strict decision and common binary inspector

The candidate binds only `usr/bin/alloy`, the exact reviewed image configuration and product labels, binary SHA256, canonical build information, exact module/version/PURL and the two IDs CVE-2026-41567 / CVE-2026-42306. Fresh raw reports retain their own SHA and are not rewritten. Duplicate matching CVEs do not receive a subtraction; a finding on another executable remains counted. Missing/changed proof, changed bytecode/build information and expiry reject. A rejection contributes no subtraction and forces FAIL in the Alloy summary. Other fixed findings remain blocking; other unfixed findings continue to require review.

The stored proof contains all 56 checks from the unchanged independent artifact review, including the direct `/usr/bin/alloy` target evidence. Proof files are individually size/hash checked with bounded decompression; graph traversal covers every recorded package, positive Docker-client controls remain present, and daemon packages/functions are explicitly rejected. Expiry is fixed at 2027-01-01 and invalid or earlier-than-review time is not accepted. The raw source closure supplements the function table's inlining limitation.

The shared inspector creates a stopped, network-isolated container, copies only the validated executable path, and reads build information using the pinned Go tool image without executing the inspected image. It compares full rootfs layers/product labels and exact binary/build bytes. The Grafana refactor keeps its previous matching and fixed-code/ancestry semantics; the scanner retains ignorefile=/dev/null, all packages, original thresholds, raw JSON, diagnostics and SBOM. The retained 50-test log covers the two matching decisions plus changed target/config/PURL/module/version/CVE, duplicate/extra executable, expired/missing/tampered proof, binary/rootfs/compiler/build-info changes, and new unrelated findings.

Scope remains exact-artifact non-affected code, not a blanket Docker library ignore, host-daemon exemption or approval of unrelated risks. Root owns the subsequent actual full scanner run and integration. No shared source changes, network/API requests, Docker/SQL operations, builds, scans or test reruns were performed by this review. `verification.json` records the precise reviewed source and evidence hashes; a later source delta requires comparison against this frozen set.
