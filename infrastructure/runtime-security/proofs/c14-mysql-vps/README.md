# Actual VPS287 release-pair rehearsal — 8 September2026

[`actual-result.json`](actual-result.json) is the hash-bound acceptance entry point: **76/76 runtime checks**, **60/60 helper unit tests**, and an independent after-run source continuity receipt. The dedicated validator additionally passes **34/34** positive/tamper cases. The original C7 publication and anonymous-download pair must be validated separately.

The actual read-only VPS capture is MySQL9.0.0 / Flyway1.10.287 with the original app configuration `dd0585…` (exact copied local manifest `bcd87b…`). The same owned data volume upgraded to the published MySQL9.7.3 candidate. Application `f6123a…` then appended17 Flyway entries, from374 to391 rows, reaching1.10.310. The full Upgrade Checker reported0 errors,23 warnings,7 completed checks and no manual checks. All warnings are recorded server-variable warnings. This is a new run; the earlier local310/C14 and historical306/C12 results remain separate evidence.

Both pre-existing UNKNOWN records in `scheduled_client_message_state` retain their complete23 original columns, including operation identity and message/route fields. The original and postmigration TSVs have SHA256 `62dcc98bd74a1644151092209ef0969504c1f1cb5d5c1453bcb9c2daee40feba`. All69 queries were executed; historical copied snapshots without primary keys are included using type-aware full binary serialization and a complete-row tie-breaker. The actual TSV contents remain in the protected private capture directory, outside this repository.

The published server ran as actual UID999, on an internal network with no host ports. GTID/enforcement OFF, binlog ON/ROW, event scheduler OFF, source collation/timezone/FK settings and the exact configured application grants were verified. Candidate writes survived a clean restart. The pre-upgrade backup restored into a different, initially empty9.0 volume; old-app health, original287 history, business/count/JSON comparisons and UNKNOWN identity all passed. The post-upgrade canary was absent after restoring the pre-upgrade recovery point. The old binary never opened the upgraded volume.

The original app has shell PID1 without `exec`. Its verified sole JVM child received TERM and completed Spring/JPA/Hikari shutdown, with exit143 and no OOM/forced kill. Original-app validation on both baseline and rollback used actual MySQL `read_only` and `super_read_only` fences to suppress its immediate shadow-backfill writes; both flags were released after graceful stop. This is health/schema/shutdown evidence under that stated fence, not a writable old-app business API test or a claim that stock Docker stop is graceful for that image.

All five early failed attempts and their concrete causes are retained in [`fixture-failures.json`](fixture-failures.json); their private original logs and metadata remain available. Run4's22 new contractor shadow coordination claims were expected background work, not corruption. No table was excluded to obtain PASS. Failed and successful disposable resources/copies were removed with ownership/path checks. The original read-only VPS capture and exact old-image archive remain protected outside the repository.

The fresh same-day security scan of the exact published image reports0 HIGH/CRITICAL findings without exclusions or adjudication. This proof is **COORDINATED_CANDIDATE_PREPARATION**: `vpsCutoverExecuted:false`, `ordinaryDeploymentUpgradeAuthorized:false`. Fresh authenticated encrypted production backup, independent copy, verified empty-volume restore and the separately reviewed coordinated cutover remain mandatory. The ordinary database image-identity guard remains in force.
