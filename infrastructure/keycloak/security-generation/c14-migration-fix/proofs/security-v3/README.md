This independent evidence is for local final v3 image `sha256:367c57af165ab4e46a36eaaea592a81575e782ce4d0a1a6695b13cdc99d4be2b`, OCI config `sha256:b1b695fb74f983c010819b25ef5f402622f20cf865fbbb31133d9a7c69b2538b`.

The fresh full Trivy image scan completed on 2026-09-08 at 15:39 UTC with 0 HIGH, 0 CRITICAL and 49 lower findings. Both Ubuntu 22.04 OS packages (143) and JAR packages (512) were analyzed. The scanner completed with a 512 MiB limit, exit 0 and OOMKilled=false; the database update was not disabled. All raw findings and the derived CycloneDX SBOM are retained. Existing Jackson 2.21.6/Parsson 1.1.9 runtime policy passes against this raw scan. No exclusion or new adjudication was used.

Stopped-image exports compared the complete root filesystem to published base `ghcr.io/claidd/otziv-security@sha256:93dd42a5379a80fc0673bc1d0c5601d3b35009c8250394b1e2b45082388c82cc`. File contents, types, modes, uid/gid and symlink targets were compared, excluding mtime and the three Docker-generated hosts/hostname/resolv.conf files. Nine paths differ: two vendor JARs, three Quarkus augmentation files, the provenance sidecar, and the proof directory with its two text files. No other runtime OS, CLI, provider or dependency file changes were found. Of 476 Keycloak JARs, 472 are byte-identical; the remaining four are the two patched modules and two augmentation JARs. Only the six compiled classes from the three intended source families changed inside the vendor modules; all 785 unrelated entries remain unchanged.

Review of ModulePatch confirms pinned original JAR hashes, class-major 61 and exact source-family checks, preservation of unrelated entries and manifests, signature rejection, and recursively sorted map serialization. ApplyPatch requires the exact three paths and exact hunk line numbers, context and counts; Dockerfile additionally checks source hashes before and after application. The actual image provenance binds the patch/source manifest and every compiler classpath JAR to the exported original bytes. ManagedModelsProof exercises the actual migration/clear boundary and commit/rollback callbacks with proxy persistence and a cache harness; it is a boundary unit proof. Actual database migration, admin-role continuity, restart and rollback tests are recorded separately by the runtime reviewer, not replaced by this scan.

The extra five-file v2/v3 comparison found both patched vendor JARs and quarkus-application.dat byte-identical. Transformed-bytecode.jar has identical uncompressed entries but different ZIP bytes. Generated-bytecode.jar has 138 differing uncompressed entries. Therefore reproducible source and vendor modules do not establish byte identity for Quarkus augmentation. A future published image must be scanned and bound to its own generated payload and actual runtime replay when that payload differs. This proof makes no claim that an arbitrary later build has already passed runtime testing or production acceptance.

`review.json` freezes evidence and the nine source build inputs. Executed Python scripts are retained verbatim as execution evidence; they used the private scratch directory recorded in the receipts. Raw files have byte-preserving Git attributes. Run the focused offline integrity checks with:

```
node --test infrastructure/keycloak/security-generation/c14-migration-fix/proofs/security-v3/security-v3.test.mjs
```
